/**
 * OEE tab — delegates to the OeeScreen organism.
 *
 * The OEE screen component lives in src/ui/components/organisms/OeeScreen.tsx
 * so it can be shared between the legacy OEE tab and the new FormRouter tab.
 */

export { default } from '../../src/ui/components/organisms/OeeScreen';
